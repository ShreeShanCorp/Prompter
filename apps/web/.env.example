VITE_API_BASE_URL="http://localhost:3001"
VITE_CLERK_PUBLISHABLE_KEY=

# E2E test account (Playwright only, never used at app runtime). Must
# already exist -- sign up for it through the app UI once; automated
# sign-up hits a Cloudflare bot check that must not be bypassed.
E2E_TEST_EMAIL=
E2E_TEST_PASSWORD=
